# Install Geode on Linux

## Run using `wget`:
```bash
wget https://github.com/GMDProjectL/install-geode-on-linux/releases/download/binary/installer -O /tmp/install-geode
chmod +x /tmp/install-geode
/tmp/install-geode
```